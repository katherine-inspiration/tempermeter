import React from "react";
import styled from "styled-components";

let About = (props) => {
    return (
        <div className={}>

        </div>
    );
};

About = styled(About)`
`;

export default About;